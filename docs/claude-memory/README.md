# Contexto para Claude Code

Estos archivos son una copia de la memoria que Claude fue guardando durante el trabajo en este proyecto (automatización de Pairings WB/NB para el Freeze de LATAM). Se copian aquí, dentro del repo, para que viajen junto con el código a cualquier computadora donde se abra este proyecto con Claude Code — la memoria original vive solo en la máquina donde se generó y no se sincroniza sola.

**Si estás retomando este proyecto en una sesión/computadora nueva:** pide a Claude que lea estos 4 archivos (empezando por `MEMORY.md`) para reconstruir el contexto completo antes de seguir trabajando.

- `MEMORY.md` — índice
- `project_pairings_overview.md` — qué es el proyecto, scripts clave, estado más reciente
- `feedback_qa_rigor_pairings.md` — qué tipo de QA se espera antes de dar algo por correcto
- `reference_pairings_manual.md` — de dónde salen las reglas de negocio, y el mapeo de columnas CSV ↔ BigQuery

Nota: si esta copia queda desactualizada respecto a la memoria real de Claude, no pasa nada grave — es un respaldo/checkpoint, no la fuente de verdad. Pídele a Claude que la actualice de vez en cuando (o dile "actualiza docs/claude-memory con el estado actual").
